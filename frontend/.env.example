# Only needed if the frontend is served from a different origin than the
# backend (e.g. a split production deploy). Leave unset for local dev - the
# Vite dev server proxy (vite.config.js) handles /api calls for you.
VITE_API_BASE_URL=
